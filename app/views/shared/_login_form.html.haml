= bootstrap_form_tag url: log_in_path, remote: true do |f|
  = f.email_field :email
  = f.password_field :password
  = f.submit "Log In", class: "btn btn-primary"
  - if modal
    %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close